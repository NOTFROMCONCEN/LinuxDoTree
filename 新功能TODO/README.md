# 新功能 TODO

这个目录用于收集 `linuxdotree` 后续可以继续加入的功能。

## 已规划

- [x] 页面内树形 / 平铺浮动切换按钮
- [x] 记忆用户偏好
- [x] 分类白名单
- [x] 折叠回复分支
- [x] 高亮父链
- [x] 浮动面板增加"全部展开 / 全部折叠"
- [x] 浮动面板增加一键复制树形链接

## 候选功能

- [ ] 记住上次阅读位置，并自动回到未读附近
- [ ] 仅在指定分类或标签下显示浮动切换按钮
- [ ] 在主题列表页直接标记"会进入树形评论"
- [ ] 为树形模式增加快捷键（如 Ctrl+Shift+T 切换模式）
- [ ] 只看楼主、只看作者、只看提及我的回复
- [ ] 提供浅色 / 深色 / 跟随系统三种扩展主题
- [ ] 高亮当前跳转目标评论（锚点自动滚动高亮）
- [ ] 导出 / 导入扩展设置
- [ ] 折叠分支状态持久化（刷新后恢复）

## 代码优化项

- [x] MutationObserver 增加自身变更过滤，避免扩展注入元素触发无意义重刷
- [x] 提取 `currentPostRecords` 为模块级变量，支持全局折叠/展开操作
- [ ] `DEFAULT_SETTINGS` 分散在三个文件维护，易遗漏同步
- [ ] popup.js 硬编码 `chrome` 对象，未走 `extensionApi` 兼容层（Firefox 兼容风险）
- [ ] `computePostTree` 在循环内混合读写布局（getBoundingClientRect），可改为先批量读取再构建树